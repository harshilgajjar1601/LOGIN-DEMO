{
  "name": "Login-form",
  "version": "1.0.0",
  "description": "This is the simple login form using Node.js, Express, MySQL, and JWT for authentication. It allows users to register, log in, and access a protected dashboard. The application securely handles user credentials and sessions using bcrypt for password hashing and JWT for token-based authentication.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Harshil Gajjar",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "body-parser": "^2.2.2",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
